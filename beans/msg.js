const serverError = 'Internal server error';

const create = 'Registro creado correctamente';

const update = 'Registro actualizado correctamente';

const deleteR = 'Registro eliminado correctamente';

const notFound = 'Registro no encontrado';

module.exports = {
    serverError,
    create,
    update,
    deleteR,
    notFound
}